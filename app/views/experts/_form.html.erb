<%= form_for(@expert) do |f| %>
  <% if @expert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@expert.errors.count, "error") %> prohibited this expert from being saved:</h2>

      <ul>
      <% @expert.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, :class => "form-control" %>
  </div>
    <%= f.fields_for :questions do |builder| %>
        <div class="form-group">
        <%= builder.label :text, "Question" %>
          <%= builder.text_field :text, :class => "form-control" %>
        </div>
    <% end %>
      <div class="actions">
    <%= f.submit :class => "btn btn-default" %>
  </div>
<% end %>
